# Core detection dependencies (CURRENTLY IMPLEMENTED)

ultralytics>=8.3.0          # YOLO11n model + Ultralytics inference engine
opencv-python>=4.9.0        # Frame reading, drawing, video I/O
                            # (use opencv-python-headless on servers without GUI)
numpy>=1.26.0               # Array operations
torch>=2.2.0                # Device auto-detection (CUDA / MPS) + model inference
                            # For GPU: pip install torch --index-url https://download.pytorch.org/whl/cu121

# Testing 
pytest>=8.0.0
pytest-cov>=5.0.0           # Coverage reporting

# Future module dependencies (NOT YET IMPLEMENTED)
# Install when the respective modules are built.

# tracking/
# boxmot>=10.0.0            # BoT-SORT / ByteTrack

# anpr/
# easyocr>=1.7.0            # Plate OCR

# face/
# deepface>=0.0.93          # Face detection + embeddings
# OR
# insightface>=0.7.3

# api_client/
# httpx>=0.27.0             # Async HTTP client
# websockets>=12.0

# night_detection/
# scipy>=1.13.0             # Connected-component labelling
